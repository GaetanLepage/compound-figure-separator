name: compfigsep
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - cudatoolkit=11.3
  - pytorch
  - cmake
  - protobuf=3.16

  - pip
  - pip:
    - -r requirements.txt
prefix: conda_env

